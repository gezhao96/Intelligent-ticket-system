# 自动化测试执行结果

执行日期：2026-08-08

执行命令：

```powershell
pytest -q
```

实际结果：

```text
.........                                                                [100%]
9 passed, 1 warning in 0.62s
```

说明：唯一 warning 来自当前 FastAPI/Starlette TestClient 依赖的弃用提示，不影响测试通过或业务行为。测试覆盖实际 FastAPI 路由、SQLAlchemy 事务与隔离 SQLite 数据库；DeepSeek 测试仅通过 MockTransport 模拟 HTTP 边界，生产运行仍要求真实 API Key。

